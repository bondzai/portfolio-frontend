export interface LabType {
    id: number;
    name: string;
    language: string;
    status: string;
    is_highlight: boolean;
    is_sleep: boolean;
    image_url: string;
    host_url: string;
}
